# install-services.yaml
---
- hosts: loadbalancers
  become: true
  tasks:
    - name: Installing apache
      apt:
        name: apache2
        state: present

- hosts: webservers
  become: true
  tasks:
    - name: Installing services
      apt:
        name:
          - apache2
          - php
        state: present
