# roles/galaxy/meta/main.yml
---
galaxy_info:
  author: James Lucktaylor
  description: Ansible 101 demo role, initialised via 'ansible-galaxy'

  license: MIT

  min_ansible_version: '2.2'

  galaxy_tags:
    - ansible101
    - galaxy
    - geerlingguy

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above, if you add dependencies to this list.
